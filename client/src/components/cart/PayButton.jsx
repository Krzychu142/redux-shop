import React from 'react';
import axios from 'axios';
import { useSelector } from 'react-redux';
import baseUrl from '../../features/api';

const PayButton = () => {
  return (
    <div>
      <button>Checko Out</button>
    </div>
  );
};

export default PayButton;
